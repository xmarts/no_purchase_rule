<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="no_purchase_rule.list">
      <field name="name">no_purchase_rule list</field>
      <field name="model">no_purchase_rule.no_purchase_rule</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="no_purchase_rule.action_window">
      <field name="name">no_purchase_rule window</field>
      <field name="res_model">no_purchase_rule.no_purchase_rule</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="no_purchase_rule.action_server">
      <field name="name">no_purchase_rule server</field>
      <field name="model_id" ref="model_no_purchase_rule_no_purchase_rule"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="no_purchase_rule" id="no_purchase_rule.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="no_purchase_rule.menu_1" parent="no_purchase_rule.menu_root"/>
    <menuitem name="Menu 2" id="no_purchase_rule.menu_2" parent="no_purchase_rule.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="no_purchase_rule.menu_1_list" parent="no_purchase_rule.menu_1"
              action="no_purchase_rule.action_window"/>
    <menuitem name="Server to list" id="no_purchase_rule" parent="no_purchase_rule.menu_2"
              action="no_purchase_rule.action_server"/>
    -->
  </data>
</odoo>